pipeline {
    agent any

    stages {
        stage('w/o docker') {
            steps {
                sh 'echo "Without docker"'
            }
        }

        
            steps {
                sh 'echo "With docker"'
                sh 'npm --version'
            }
        }
    }
